<div class="col s12">
    <div class="card">
        <div class="card-header">
            <span class="card-title">{{name}}</span>
            <span class="date-posted">{{date_created}}</span>
        </div>
        <div class="card-content">
            <p>{{content}}</p>
        </div>
    </div>
</div>